<UserControl x:Class="WebView22Browser.App.Views.ExtensionsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#CCC" BorderThickness="1,0,0,0" Background="#F5F5F5">
        <DockPanel Margin="8">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="扩展程序" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="折叠"
                            Margin="8,0,0,0"
                            Padding="6,2"
                            Command="{Binding TogglePanelCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           FontSize="11"
                           Foreground="#666"/>
            </StackPanel>
            <Button DockPanel.Dock="Top"
                    Content="从文件夹安装…"
                    Margin="0,0,0,8"
                    Padding="6,4"
                    Command="{Binding InstallFromFolderCommand}"/>
            <ListBox ItemsSource="{Binding Items}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="2" Margin="0,0,0,4">
                            <StackPanel>
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Right"
                                              Content="启用"
                                              IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                              Command="{Binding DataContext.ToggleEnabledCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"
                                              VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                                <TextBlock Text="{Binding Id}"
                                           FontSize="10"
                                           Foreground="#888"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,2,0,0"/>
                                <Button Content="移除"
                                        HorizontalAlignment="Left"
                                        Margin="0,4,0,0"
                                        Padding="8,2"
                                        Command="{Binding DataContext.RemoveExtensionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</UserControl>
