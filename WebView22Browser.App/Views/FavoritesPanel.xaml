<UserControl x:Class="WebView22Browser.App.Views.FavoritesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#CCC" BorderThickness="0,0,1,0" Background="#F5F5F5">
        <DockPanel Margin="8">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="收藏夹" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="折叠"
                        Margin="8,0,0,0"
                        Padding="6,2"
                        Command="{Binding TogglePanelCommand}"/>
            </StackPanel>
            <ListBox ItemsSource="{Binding Items}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="2">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding DataContext.OpenFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <DockPanel>
                                <Button DockPanel.Dock="Right"
                                        Content="×"
                                        Width="24"
                                        Margin="4,0,0,0"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</UserControl>
